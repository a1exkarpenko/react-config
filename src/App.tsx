import React, { FC } from 'react'
import { BrowserRouter } from 'react-router-dom'

const App: FC = () => {
    return (
        <BrowserRouter>
            Hello World!
        </BrowserRouter>
    )
}

export default App
